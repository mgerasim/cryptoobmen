
<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap3-wysiwyg/0.3.3/amd/bootstrap3-wysihtml5.all.min.js"></script>
<%= form_with(model: setting, local: true, :html => {class: 'form-horizontal'}) do |form| %>
  <% if setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(setting.errors.count, "error") %> prohibited this setting from being saved:</h2>

      <ul>
      <% setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Правила обмена", :class => "col-sm-3 control-label" %>
    <%= form.text_area :pravila_obmena, id: :setting_pravila_obmena, :class => "form-control wysihtml5" %>
  </div>

<script type="text/javascript">
  $(document).ready(function(){

    $('.wysihtml5').each(function(i, elem) {
      $(elem).wysihtml5();
    });

  })
</script>


  <div class="form-group">
    <%= form.label "Контакты", :class => "col-sm-3 control-label" %>
    <%= form.text_area :contact, id: :setting_contact, :class => "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label "О сервисе", :class => "col-sm-3 control-label" %>
    <%= form.text_area :about, id: :setting_about, :class => "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit "Сохранить", :class => "btn btn-primary" %>
  </div>
<% end %>
