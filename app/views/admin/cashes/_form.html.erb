<%= form_with(model: admin_cash, local: true) do |form| %>
  <% if admin_cash.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_cash.errors.count, "error") %> prohibited this admin_cash from being saved:</h2>

      <ul>
      <% admin_cash.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code, id: :admin_cash_code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :admin_cash_name %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon, id: :admin_cash_icon %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.number_field :category, id: :admin_cash_category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
